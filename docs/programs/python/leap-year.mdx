---
sidebar_position: 1
---

# うるう年か判定する関数

Pythonで引数に指定されたint値がうるう年であるかを判定し、boolで返す関数です。
is_leap_yearにint型で年を渡すと、Booleanで返ってきます。

```python
def is_leap_year(year: int):
    if (year % 4 == 0 and year % 100 != 0) or year % 400 == 0:
        return True
    else:
        return False
```
